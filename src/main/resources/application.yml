spring:
  datasource:
    url: jdbc:mysql://localhost:3306/softbinator
    username: root
    password: parola
    platform: mysql
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  # Hibernate ddl auto (create, create-drop, validate, update)